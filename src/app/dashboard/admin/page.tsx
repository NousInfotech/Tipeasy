import { NextResponse } from 'next/server';
import * as React from 'react';
import { PageContainer } from '@toolpad/core/PageContainer';
export default function AdminPage() {
    return (
        <PageContainer>Admin Profile Page</PageContainer>
    );
}
